{
  "name": "fuel-sales-app-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright smoke/UI tests for the Fuel-Sales-App static HTML apps.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
