# Copy this file to .env and fill in real values if you want a local
# reference — but .env is gitignored and NOT auto-loaded by these tests.
# The tests read credentials from real environment variables (see README.md)
# so nothing sensitive ever needs to touch disk in this folder.
TEST_ADMIN_EMAIL=test-admin@skelsee.co.zw
TEST_ADMIN_PASSWORD=
